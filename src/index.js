import _ from "lodash";
import "./style.css";

import addClientValidation from "./formValidate";
addClientValidation();
let submitBtn = document.getElementById('submitBtn');
submitBtn.addEventListener('click', () => {

});
